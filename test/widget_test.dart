import 'package:flutter_test/flutter_test.dart';
import 'package:voice_spell/main.dart';

void main() {
  test('VoiceSpellApp class is defined', () {
    // Verifies compilation of main app
    expect(VoiceSpellApp, isNotNull);
  });
}
